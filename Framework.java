import java.util.*;
public class Framework {
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        // Array List
        // List<String> studentname=new ArrayList<>();
        // studentname.add("Rakesh");
        // studentname.add(1, "Ajay");
        // studentname.addAll(studentname);
        // System.out.println(studentname.get(0));
        // studentname.remove(0);
        // studentname.remove(String.valueOf("Rakesh"));
        // studentname.clear();
        // studentname.set(1, "Ajayk");
        // studentname.contains(200);
        // studentname.size();
        // studentname.get(1);


        // stack
        // Stack<Integer> num1=new Stack<>();
        // num1.push(null);
        // num1.pop();
        // num1.peek();


        // Queue<Integer> q1=new LinkedList<>();
        // q1.offer(2);
        // q1.poll();
        // q1.peek();

        // PriorityQueue<Integer> pq=new PriorityQueue<>(Comparator.reverseOrder());
        // change the order;
        // pq.offer(2);
        // pq.poll();
        // pq.peek();

        // ArrayDeque<Integer> adq=new ArrayDeque<>();
        // adq.offer(13);
        // adq.offerFirst(12);
        // adq.offerLast(13);
        // System.out.println(adq);
        // adq.peek();
        // adq.peekFirst();
        // adq.peekLast();
        // adq.poll();
        // adq.pollFirst();
        // adq.pollLast();

        // Set<Integer> st=new HashSet<>();
        // st.add(12);
        // st.contains(12);
        // st.remove(12);
        // st.isEmpty();
        // st.size();
        // st.clear();
        
        
    }
}
